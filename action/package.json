{
  "name": "envcheck-action",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub Action for envcheck",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
